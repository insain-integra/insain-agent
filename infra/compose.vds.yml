# Продакшен на VDS: API только на localhost, наружу — через nginx (80/443).
# Запуск: docker compose -f docker-compose.yml -f infra/compose.vds.yml up -d --build

services:
  calc-api:
    ports:
      - "127.0.0.1:8001:8001"
